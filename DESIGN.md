# 2단계 설계 문서 — A+ Escape: IT4호관 탈출

## 1. 프로젝트 개요

`A+ Escape: IT4호관 탈출`은 Python 개념 아이템을 수집하고 감시자를 피해 교수님에게 A+를 받는 pygame 기반 2D 잠입/탈출 게임이다.
플레이어는 층별 복도와 방을 오가며 아이템을 모으고, 자습실 학생과 수업 중 교수님, 복도 경비를 피해 이동한다.

이 문서는 현재 구현된 코드 구조를 기준으로 기능, 데이터 구조, 함수, 상태 흐름을 정리한다.

## 2. 기능 목록

### MUST

```text
[MUST] pygame 창 생성 및 게임 루프 실행
[MUST] 플레이어 이동
[MUST] 층별 복도 출력
[MUST] 방 입장/퇴장
[MUST] 엘리베이터 층 이동
[MUST] 아이템 수집
[MUST] 아이템 수집 개수 UI 표시
[MUST] 자습실 학생 랜덤 뒤돌아보기
[MUST] 수업 중 교수님 부채꼴 시야 판정
[MUST] 경비아저씨 손전등 판정
[MUST] 게임오버 처리
[MUST] 교수실 대화
[MUST] 모든 아이템 수집 후 게임 클리어
[MUST] R 키 재시작
```

### NICE

```text
[NICE] 배경음악 적용
[NICE] 캐릭터/학생/교수님/경비 스프라이트 적용
[NICE] 교수님 대화창 연출
[NICE] 학생들이 뒤돌아보는 연출
[NICE] 디버그 히트박스 설정
```

### LATER

```text
[LATER] 시작 화면
[LATER] 난이도 선택
[LATER] 아이템별 Python 개념 설명
[LATER] 퀴즈 기능
[LATER] 저장/불러오기
[LATER] 효과음 추가
```

## 3. 데이터 구조 설계

### player

```python
player = {
    "x": 80,
    "y": 500,
    "width": 30,
    "height": 34,
    "speed": 4,
    "facing": "front",
}
```

플레이어의 현재 좌표, 충돌 크기, 이동 속도, 바라보는 방향을 저장한다. 방향 값은 이동 스프라이트를 고르는 데 사용된다.

### item

```python
item = {
    "name": "변수 노트",
    "rect": pygame.Rect(x, y, 34, 34),
    "symbol": "x",
    "color": GREEN,
    "collected": False,
}
```

아이템 이름, 위치, 기본 표시 정보, 수집 여부를 저장한다. 실제 화면에서는 PNG 에셋이 있으면 이미지로 그리고, 없으면 색상과 기호로 표시한다.

### student

```python
student = {
    "rect": pygame.Rect(x, y, 44, 60),
    "state": "studying",
    "timer": 0,
    "look_duration": 65,
    "normal_image_key": "boy_studyroom",
    "look_image_key": "boy_studyroom_look",
    "student_type": "studyroom",
}
```

학생의 위치, 현재 상태, 뒤돌아보는 시간, 사용할 이미지 키를 저장한다. 자습실 학생은 랜덤으로 `looking` 상태가 되고, 수업 중 학생은 교수님에게 들켰을 때 모두 뒤돌아보는 연출에 사용된다.

### professor

```python
professor = {
    "rect": pygame.Rect(455, 245, 60, 80),
    "state": "teaching",
    "normal_image_key": "professor_classroom",
    "alert_image_key": "professor_classroom_angry",
    "sight_offset_x": 0.0,
    "sight_speed": 0.8,
    "sight_length": 145,
    "sight_half_width": 115,
}
```

수업 중 강의실 교수님의 위치와 시야 정보를 저장한다. 교수님 몸은 고정되어 있고, `sight_offset_x` 값이 바뀌면서 부채꼴 시야가 좌우로 움직인다.

### room

```python
room = {
    "id": "105",
    "name": "105호 수업 중 강의실",
    "door_rect": pygame.Rect(...),
    "entry_rect": pygame.Rect(...),
    "kind": "professor_trap",
    "items": [],
    "students": [],
    "professor": professor,
    "desks": [],
}
```

방의 번호, 이름, 복도에서의 문 위치, 실제 입장 가능 영역, 방 종류, 내부 오브젝트를 저장한다. `kind` 값에 따라 빈 강의실, 자습실, 수업 중 강의실, 교수실 동작이 달라진다.

### floors

```python
floors = {
    1: {
        "name": "1층",
        "start": (80, 500),
        "rooms": [],
        "guards": [],
    },
    2: {...},
    3: {...},
}
```

층별 시작 위치, 방 목록, 경비 목록을 저장한다. 현재 층 번호를 기준으로 복도 배경, 방, 경비를 선택해 화면에 출력한다.

## 4. 주요 함수 설계

### move_player(keys, allowed_rect, walls)

역할:
키 입력에 따라 플레이어를 이동시키고, 이동 가능 영역과 장애물 충돌을 확인한다.

의사코드:
1. 방향키 또는 WASD 입력을 확인한다.
2. 입력에 따라 dx, dy 값을 정한다.
3. x축 이동을 먼저 시도한다.
4. 이동 후 위치가 허용 영역 안이고 벽과 충돌하지 않으면 x좌표를 갱신한다.
5. y축 이동도 같은 방식으로 처리한다.
6. 실제 이동이 일어났는지 True/False로 반환한다.

### find_near_room()

역할:
플레이어가 현재 층의 어떤 방 입장 영역과 겹치는지 확인한다.

의사코드:
1. 플레이어 충돌 사각형을 만든다.
2. 현재 층의 방 목록을 순회한다.
3. 플레이어가 방의 `entry_rect`와 충돌하면 해당 방을 반환한다.
4. 겹치는 방이 없으면 `None`을 반환한다.

### enter_room(room)

역할:
선택한 방으로 플레이어를 이동시키고 방 내부 상태를 초기화한다.

의사코드:
1. 현재 방을 `room`으로 설정한다.
2. 복도로 돌아갈 위치를 저장한다.
3. 학생 상태와 교수님 시야 상태를 초기화한다.
4. 플레이어를 방 내부 시작 위치로 이동시킨다.

### exit_room()

역할:
현재 방에서 복도로 돌아간다.

의사코드:
1. 현재 방 값을 비운다.
2. 저장해 둔 복도 위치로 플레이어 좌표를 되돌린다.
3. 복귀 안내 메시지를 표시한다.

### check_item_collision()

역할:
플레이어와 아이템이 충돌했는지 확인하고, 충돌한 아이템을 수집 처리한다.

의사코드:
1. 현재 방이 없으면 처리하지 않는다.
2. 현재 방의 아이템 목록을 순회한다.
3. 수집되지 않은 아이템과 플레이어가 충돌했는지 확인한다.
4. 충돌하면 `collected`를 True로 바꾼다.
5. 수집 메시지를 표시한다.

### update_studyroom_students(room, player_moved)

역할:
자습실 학생이 랜덤으로 뒤돌아보는 동작과 발각 판정을 처리한다.

의사코드:
1. 방의 타이머를 증가시킨다.
2. 정해진 시간이 되면 학생 중 한 명을 `looking` 상태로 바꾼다.
3. 뒤돌아보는 시간이 끝나면 다시 `studying` 상태로 되돌린다.
4. 학생이 보고 있을 때 플레이어가 움직이면 게임오버를 발생시킨다.

### update_professor_sight_sweep(prof)

역할:
수업 중 교수님의 시야가 좌우로 움직이도록 offset 값을 갱신한다.

의사코드:
1. 현재 방향과 속도에 따라 `sight_offset_x`를 갱신한다.
2. 최소 범위보다 작아지면 최소값으로 고정하고 방향을 바꾼다.
3. 최대 범위보다 커지면 최대값으로 고정하고 방향을 바꾼다.

### professor_sight_points(prof)

역할:
교수님 시야를 삼각형 polygon으로 만들기 위한 세 점을 계산한다.

의사코드:
1. 교수님 위치에서 시야 시작점을 정한다.
2. `sight_offset_x`, `sight_length`, `sight_half_width`를 읽는다.
3. 아래쪽 왼쪽 점과 오른쪽 점을 계산한다.
4. 세 점 리스트를 반환한다.

### player_in_professor_sight(prof)

역할:
플레이어가 교수님의 삼각형 시야 안에 들어갔는지 판정한다.

의사코드:
1. 교수님 시야 삼각형 점을 계산한다.
2. 플레이어 중심점과 네 꼭짓점을 검사 후보로 만든다.
3. 후보 점 중 하나라도 삼각형 안에 있으면 True를 반환한다.
4. 모두 밖에 있으면 False를 반환한다.

### update_corridor_guard()

역할:
복도 경비아저씨를 이동시키고 손전등 충돌을 검사한다.

의사코드:
1. 현재 층의 경비 목록을 순회한다.
2. 경비를 현재 방향으로 이동시킨다.
3. 순찰 범위 끝에 닿으면 방향을 반대로 바꾼다.
4. 손전등 사각형과 플레이어가 충돌하면 게임오버를 발생시킨다.

### start_professor_dialog(text, result)

역할:
교수실 대화 상태를 시작한다.

의사코드:
1. 표시할 대사 문자열을 저장한다.
2. 대화 결과 값을 저장한다.
3. `game_state`를 `professor_dialog`로 바꾼다.

### draw_corridor()

역할:
현재 층 복도 화면을 그린다.

의사코드:
1. 현재 층의 복도 배경 이미지를 그린다.
2. 방 문과 엘리베이터를 표시한다.
3. 경비와 손전등을 그린다.
4. 플레이어와 상단 UI를 그린다.

### draw_room()

역할:
현재 방 내부 화면을 그린다.

의사코드:
1. 방 종류에 맞는 배경 이미지를 선택해 그린다.
2. 나가기 영역을 처리한다.
3. 수업 중 강의실이면 교수님 시야를 먼저 그린다.
4. 학생, 교수님, 아이템, 플레이어를 순서대로 그린다.
5. 상단 UI를 그린다.

### draw_ui()

역할:
현재 위치, 수집 아이템 개수, 안내 메시지를 화면에 표시한다.

의사코드:
1. 수집된 아이템 개수를 계산한다.
2. 상단 바 왼쪽에 현재 위치를 표시한다.
3. 상단 바 오른쪽에 아이템 수집 개수를 표시한다.
4. 모든 아이템을 모으면 교수실 안내 메시지를 강조 표시한다.
5. 하단 메시지가 있으면 배경 박스와 함께 출력한다.

### reset_game(show_message=True)

역할:
게임을 처음 상태로 초기화한다.

의사코드:
1. 현재 층과 현재 방을 초기값으로 되돌린다.
2. 플레이어 위치와 방향을 초기화한다.
3. 모든 아이템 수집 상태를 False로 바꾼다.
4. 학생, 교수님, 경비 상태를 초기화한다.
5. 배경음악을 다시 재생한다.

## 5. 게임 상태 설계

```text
playing: 기본 게임 진행 상태
board_menu: 엘리베이터 층 선택 메뉴
professor_dialog: 교수실 교수님 대화창 상태
caught_animation: 교수님에게 들켰을 때 학생들이 뒤돌아보는 짧은 연출 상태
caught: 게임오버 상태
cleared: 게임 클리어 상태
```

상태 전환 흐름:

```text
playing
 ├─ 엘리베이터 앞 SPACE → board_menu
 ├─ 교수님 대화 → professor_dialog
 ├─ 감시자에게 들킴 → caught 또는 caught_animation
 └─ 모든 아이템 수집 후 교수님 대화 완료 → cleared

board_menu
 ├─ 1/2/3 입력 → playing
 └─ ESC → playing

professor_dialog
 └─ SPACE → playing 또는 cleared

caught_animation
 └─ 타이머 종료 → caught

caught
 └─ R → playing

cleared
 └─ R → playing
```

## 6. 화면 흐름 설계

```text
[게임 시작]
    ↓
[복도 화면]
    ├─ 문 앞 SPACE → [방 내부]
    ├─ 엘리베이터 SPACE → [층 선택 메뉴]
    └─ 경비 손전등 충돌 → [게임오버]

[방 내부]
    ├─ 빈 강의실 → 아이템 확인 / 방 나가기
    ├─ 자습실 → 학생 뒤돌아보기 회피
    ├─ 수업 중 강의실 → 교수님 시야 회피
    └─ 교수실 → 교수님 대화

[교수실 대화]
    ├─ 아이템 부족 → 부족 대사 후 게임 계속
    └─ 아이템 모두 수집 → A+ 대사 후 클리어
```

## 7. 방 종류별 동작 설계

| kind | 의미 | 주요 동작 |
|---|---|---|
| `empty` | 빈 강의실 | 학생/교수 없음, 아이템이 있을 수 있음 |
| `students` | 자습실 | 학생 중 한 명이 랜덤으로 뒤돌아봄 |
| `professor_trap` | 수업 중 강의실 | 교수님 부채꼴 시야에 들어가면 발각 |
| `goal_professor` | 교수실 | 최종 대화 및 클리어 판정 |

## 8. 충돌 판정 설계

1. 플레이어와 벽 충돌  
   이동 후 플레이어 사각형이 이동 가능 영역을 벗어나거나 벽 사각형과 겹치면 이동을 취소한다.

2. 플레이어와 아이템 충돌  
   플레이어 사각형과 아이템 `rect`가 겹치면 아이템을 수집 처리한다.

3. 플레이어와 방 입장 영역 충돌  
   복도에서 플레이어가 방의 `entry_rect`와 겹친 상태로 `SPACE`를 누르면 방에 입장한다.

4. 플레이어와 방 나가기 영역 충돌  
   방 내부에서 플레이어가 나가기 영역과 겹친 상태로 `SPACE`를 누르면 복도로 돌아간다.

5. 플레이어와 학생 충돌  
   학생의 충돌 영역을 벽처럼 처리해 플레이어가 학생과 겹쳐 지나가지 못하게 한다.

6. 플레이어와 경비 손전등 충돌  
   경비의 방향에 따라 손전등 사각형을 만들고, 플레이어와 겹치면 게임오버를 발생시킨다.

7. 플레이어와 교수님 삼각형 시야 충돌  
   교수님 시야는 `pygame.draw.polygon()`으로 시각화한다. 플레이어의 중심점과 네 꼭짓점 중 하나라도 삼각형 안에 들어가면 발각되는 구조이다.

## 9. 리소스 관리 설계

### assets_loader.py

이미지를 안전하게 불러오고, 방 종류에 따라 적절한 배경 이미지를 선택한다. 파일이 없을 때 게임이 바로 중단되지 않도록 `safe_load_image()`를 사용한다.

### game_state.py

플레이어 위치, 현재 층, 현재 방, 메시지, 대화 상태처럼 실행 중에 계속 바뀌는 값을 한 곳에서 관리한다.

### mechanics.py

플레이어 이동, 충돌 판정, 아이템 수집, 학생/교수님/경비 기믹을 처리한다.

### render.py

복도, 방 내부, UI, 게임오버, 클리어 화면, 교수님 대화창을 그린다.

### audio.py

배경음악과 클리어 음악 재생을 담당한다.

### config.py

화면 크기, 색상, 스프라이트 크기, 경비 손전등 크기, 디버그 설정 같은 상수를 한 곳에서 관리한다.

### assets/

복도 배경, 방 배경, 플레이어, 학생, 교수님, 경비, 아이템, BGM 파일을 저장한다. 이미지 리소스는 `assets_loader.py`, 음악 리소스는 `audio.py`에서 상대 경로를 기준으로 불러온다.

## 10. 확장 가능성

추후 확장 가능 기능:

- Python 개념 퀴즈 추가
- 난이도 선택
- 저장/불러오기
- 시작 화면
- 효과음
- 더 다양한 방 기믹
- 아이템 설명 팝업
