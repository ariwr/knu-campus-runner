# A+ Escape: IT4호관 탈출

## 1. 프로젝트 소개

`A+ Escape: IT4호관 탈출`은 경북대학교 IT4호관을 배경으로 한 pygame 기반 2D 잠입/탈출 게임입니다.
플레이어는 건물 안을 돌아다니며 컴퓨팅사고와 SW코딩 수업에서 배운 Python 개념 아이템을 모으고, 최종적으로 3층 교수실의 교수님에게 A+를 받는 것을 목표로 합니다.

단순히 아이템을 줍는 게임이 아니라 자습실 학생, 수업 중 교수님, 복도 경비아저씨를 피해야 하는 잠입 요소를 포함합니다. Python 기초 문법을 게임의 목표와 규칙으로 연결해 보려는 프로젝트입니다.

## 2. 제작 동기

처음에는 학교 건물을 배경으로 한 3D 게임을 생각했지만, 프로젝트 기간과 Python 기초 수준을 고려해 2D pygame 게임으로 범위를 조정했습니다. 2D 방식은 구현 난이도를 줄이면서도 이동, 충돌, 상태 관리, UI 출력 같은 프로그래밍 요소를 충분히 담을 수 있었습니다.

Python 기초 문법을 단순 문제 풀이가 아니라 게임 목표와 연결하고 싶었습니다. 그래서 변수, 조건문, 반복문, 함수, 딕셔너리 같은 개념을 아이템으로 표현했고, 학교 공간과 수업 상황을 자습실 학생, 수업 중 교수님, 경비 순찰 같은 게임 기믹으로 재해석했습니다.

## 3. 게임 목표

플레이어는 IT4호관의 1층, 2층, 3층을 이동하며 Python 개념 아이템을 수집합니다.
아이템을 모두 모은 뒤 3층 교수실로 가서 교수님에게 말을 걸면 A+를 받고 게임을 클리어합니다.
자습실 학생, 수업 중 교수님, 복도 경비아저씨에게 들키면 게임오버가 됩니다.

## 4. 주요 기능

### 1. 층 이동과 방 탐색

- 플레이어는 1층, 2층, 3층을 이동할 수 있습니다.
- 복도에서 문 앞에 서서 `SPACE`를 누르면 해당 방에 입장합니다.
- 엘리베이터 앞에서 `SPACE`를 누르면 층 선택 메뉴가 열립니다.
- 방 안에서는 `SPACE`로 복도에 나올 수 있습니다.

### 2. Python 개념 아이템 수집

- 변수 노트, 조건문 카드, 반복문 배지, 함수 열쇠, 딕셔너리 지도를 수집합니다.
- 플레이어와 아이템이 충돌하면 자동으로 수집됩니다.
- 화면 상단에 현재 수집한 아이템 개수가 표시됩니다.
- 모든 아이템을 모으면 3층 교수실로 가라는 안내가 표시됩니다.

### 3. 자습실 학생 기믹

- 자습실에서는 학생 중 한 명이 랜덤으로 뒤돌아봅니다.
- 학생이 뒤돌아본 상태에서 플레이어가 움직이면 게임오버가 됩니다.
- 학생 스프라이트는 평소 모습과 놀란 모습을 구분해서 출력합니다.

### 4. 수업 중 강의실 교수님 기믹

- 수업 중 강의실에는 교수님과 학생들이 있습니다.
- 교수님은 교탁 앞에 고정되어 있고, 교수님의 부채꼴 시야가 좌우로 움직입니다.
- 플레이어가 교수님 시야에 들어가면 학생들이 모두 뒤돌아보고 게임오버가 됩니다.

### 5. 경비아저씨 손전등 기믹

- 2층과 3층 복도에는 경비아저씨가 순찰합니다.
- 경비아저씨는 좌우로 이동하고, 이동 방향에 따라 다른 스프라이트를 사용합니다.
- 손전등 범위에 플레이어가 들어가면 게임오버가 됩니다.

### 6. 교수실 대화와 클리어

- 아이템을 모두 모은 상태로 교수님에게 말을 걸면 A+ 대사가 출력되고 게임 클리어가 됩니다.
- 아이템을 다 모으지 않았다면 교수님이 아직 부족하다는 대사를 합니다.
- 클리어 화면에는 `A+ GAME CLEAR!`가 표시됩니다.

### 7. 그래픽과 사운드

- 복도, 강의실, 자습실, 교수실 배경 이미지를 사용합니다.
- 플레이어, 학생, 교수님, 경비아저씨, 아이템 스프라이트를 사용합니다.
- 게임 진행 중에는 `background.mp3`가 반복 재생되고, 클리어 시 `gameclear.mp3`가 재생됩니다.

## 5. 실행 방법

### 1. Python 설치

Python 3.10 이상을 권장합니다.

### 2. 필요한 라이브러리 설치

```bash
pip install pygame pillow
```

프로젝트에서는 pygame-ce 환경을 기준으로 테스트했습니다.

### 3. 실행

```bash
python main.py
```

### 4. 주의 사항

- `assets/` 폴더가 `main.py`와 같은 프로젝트 폴더 안에 있어야 합니다.
- 이미지 파일 경로가 바뀌면 `assets_loader.py`, BGM 경로가 바뀌면 `audio.py`에서 경로를 수정해야 합니다.
- 사운드 장치가 없는 환경에서는 BGM이 재생되지 않을 수 있지만, 게임은 계속 실행되도록 처리했습니다.

## 6. 조작법

| 키 | 기능 |
|---|---|
| 방향키 / WASD | 플레이어 이동 |
| SPACE | 방 입장, 방 나가기, 엘리베이터 사용, 교수님과 대화 |
| 1 / 2 / 3 | 엘리베이터 메뉴에서 층 선택 |
| ESC | 엘리베이터 메뉴 닫기 |
| R | 게임 재시작 |

## 7. 프로젝트 폴더 구조

```text
computingSW_project/
├── main.py
├── game_state.py
├── mechanics.py
├── render.py
├── audio.py
├── game_data.py
├── config.py
├── assets_loader.py
├── README.md
├── project_proposal.md
├── DESIGN.md
│
├── assets/
│   ├── bgm/
│   │   ├── background.mp3
│   │   └── gameclear.mp3
│   ├── rooms/
│   ├── passageway/
│   ├── player/
│   ├── students/
│   ├── professor/
│   ├── security/
│   └── items/
```

## 8. 주요 파일 설명

| 파일 | 역할 |
|---|---|
| `main.py` | pygame 초기화, 이벤트 처리, 업데이트 호출, 화면 갱신을 담당하는 메인 루프 |
| `game_state.py` | 플레이어, 현재 층, 현재 방, 게임 상태, 메시지 등 실행 중 상태 관리 |
| `mechanics.py` | 이동, 충돌 판정, 아이템 수집, 학생/교수님/경비 기믹 처리 |
| `render.py` | 복도, 방, UI, 게임오버, 클리어, 교수님 대화창 화면 출력 |
| `audio.py` | 배경음악과 클리어 음악 재생 |
| `game_data.py` | 층, 방, 아이템, 학생, 교수님, 경비 데이터 정의 |
| `config.py` | 화면 크기, 색상, 스프라이트 크기, 디버그 설정 등 상수 관리 |
| `assets_loader.py` | 이미지 파일 로드와 방 종류별 배경 선택 |
| `assets/` | 배경, 캐릭터, 아이템, BGM 리소스 저장 |
| `README.md` | 프로젝트 설명, 실행 방법, 테스트 방법, AI 사용 내역 정리 |
| `project_proposal.md` | 프로젝트 설명, 실행 방법, 테스트 방법, AI 사용 내역 정리 |
| `DESIGN.md` | 현재 구현 기준의 설계 문서 |

## 9. 사용한 Python 개념

| Python 개념 | 사용 예시 |
|---|---|
| 변수 | 플레이어 위치, 현재 층, 현재 방, 게임 상태 저장 |
| 조건문 | 아이템 수집 여부, 게임오버/클리어 여부, 입력 키 처리 |
| 반복문 | 게임 루프, 이벤트 처리, 아이템/학생/경비 순회 |
| 함수 | 이동, 충돌 판정, 화면 출력, 방 입장, 게임 초기화 기능 분리 |
| 리스트 | 여러 방, 아이템, 학생, 경비 데이터를 저장 |
| 딕셔너리 | 플레이어, 방, 아이템, 학생, 교수님 정보를 구조화 |
| 문자열 | 안내 메시지, 대사, 방 이름, 아이템 이름 표시 |
| random 모듈 | 자습실 학생이 랜덤으로 뒤돌아보는 동작 구현 |
| pygame.Rect | 플레이어, 아이템, 문, 시야, 충돌 영역 관리 |
| pygame.mixer | 배경음악과 클리어 음악 재생 |

## 10. 게임 진행 흐름

```text
게임 시작
  ↓
복도 탐색
  ↓
방 입장
  ├─ 빈 강의실: 아이템 확인
  ├─ 자습실: 학생 뒤돌아보기 회피
  └─ 수업 중 강의실: 교수님 시야 회피
  ↓
아이템 수집
  ↓
엘리베이터로 층 이동
  ↓
3층 교수실 입장
  ↓
교수님과 대화
  ├─ 아이템 부족: 부족하다는 대사
  └─ 아이템 모두 수집: A+ 대사 후 게임 클리어
```

## 11. 테스트 방법

| 번호 | 테스트 내용 | 기대 결과 |
|---|---|---|
| 1 | `python main.py` 실행 | 게임 창이 정상적으로 열린다 |
| 2 | 방향키/WASD 입력 | 플레이어가 이동한다 |
| 3 | 문 앞에서 `SPACE` | 해당 방에 입장한다 |
| 4 | 방 안에서 `SPACE` | 복도로 나온다 |
| 5 | 아이템과 충돌 | 아이템이 수집되고 개수가 증가한다 |
| 6 | 자습실 학생이 뒤돌아볼 때 이동 | 게임오버가 된다 |
| 7 | 교수님 시야에 들어감 | 발각 연출 후 게임오버가 된다 |
| 8 | 경비 손전등에 닿음 | 게임오버가 된다 |
| 9 | 모든 아이템 수집 후 교수님 대화 | 게임 클리어 화면이 출력된다 |
| 10 | `R` 키 입력 | 게임이 처음 상태로 초기화된다 |
| 11 | 게임 시작 후 BGM 확인 | 배경음악이 반복 재생된다 |
| 12 | 게임 클리어 | 클리어 음악이 재생된다 |

## 12. AI 도구 사용 내역

1. ChatGPT
   - 어디에 사용: pygame 기본 구조와 게임 루프 구성 방식 검토
   - 받은 도움: 입력 처리, 화면 갱신, 충돌 판정 구조 설명
   - 내 판단: 프로젝트 구조에 맞게 직접 수정하여 적용함

2. ChatGPT
   - 어디에 사용: 자습실 학생이 랜덤으로 뒤돌아보는 기믹 설계
   - 받은 도움: 학생 상태를 `studying`과 `looking`으로 나누는 방식 제안
   - 내 판단: `game_data.py`의 학생 딕셔너리와 `mechanics.py`의 업데이트 함수로 구현함

3. ChatGPT
   - 어디에 사용: 교수님 부채꼴 시야 판정 방식 설계
   - 받은 도움: 삼각형 polygon 시야와 point-in-triangle 판정 구조 설명
   - 내 판단: 교수님 몸은 고정하고 시야만 좌우로 움직이도록 수정함

4. ChatGPT
   - 어디에 사용: 스프라이트 이미지 적용과 배경 제거 방법 검토
   - 받은 도움: 투명 PNG 처리, 흰색/체크무늬 배경 제거 방식 안내
   - 내 판단: 생성된 이미지를 게임 화면에 맞게 크기와 투명도를 조정해 적용함

5. ChatGPT
   - 어디에 사용: 코드 구조 분리와 문서 정리 방향 검토
   - 받은 도움: 단일 파일 코드를 `config.py`, `game_data.py`, `assets_loader.py`로 나누는 방향과 최종 문서 목차 제안
   - 내 판단: 실제 구현 내용에 맞춰 필요한 부분만 수정하고 문서화함

## 13. 한계와 다음 단계

현재 버전은 pygame 기반의 2D 게임으로 구현되어 있어 실제 IT4호관 전체 구조를 완전히 재현하지는 못했습니다. 또한 방 내부의 상호작용은 아이템 수집과 감시자 회피 중심으로 구성되어 있으며, Python 개념 자체를 퀴즈로 풀어보는 기능은 포함되어 있지 않습니다.

추후에는 다음 기능을 추가할 수 있습니다.

- Python 개념 퀴즈 방 추가
- 아이템별 설명 팝업 추가
- 난이도 선택 기능
- 저장/불러오기 기능
- 시작 화면과 설정 화면 추가
- 효과음 추가
- 더 다양한 학생/교수님 애니메이션 추가
